     letExp(
      decList(
       varDec(a,
        nilExp(),
        TRUE),
       decList()),
      seqExp(
       expList(
        opExp(
         PLUS,
         varExp(
          simpleVar(a)),
         intExp(3)),
        expList())))