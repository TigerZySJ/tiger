     letExp(
      decList(
       typeDec(
        nametyList(
         namety(rectype,
          recordTy(
           fieldList(
            field(name,
             string,
             TRUE),
            fieldList(
             field(id,
              int,
              TRUE),
             fieldList())))),
         nametyList())),
       decList(
        varDec(a,
         rectype,
         recordExp(rectype,
          efieldList(
           efield(name,
            stringExp(hello)),
           efieldList(
            efield(id,
             intExp(1)),
            efieldList()))),
         TRUE),
        decList(
         varDec(b,
          rectype,
          nilExp(),
          TRUE),
         decList()))),
      seqExp(
       expList(
        opExp(
         EQUAL,
         varExp(
          simpleVar(b)),
         nilExp()),
        expList(
         opExp(
          NOTEQUAL,
          varExp(
           simpleVar(b)),
          nilExp()),
         expList()))))